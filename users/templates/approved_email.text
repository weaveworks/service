Your Weave Scope account has been approved!

Please follow this link to access your Scope:

    {{.LoginURL}}

Your login will be valid for 60 days.

You need to run probes on your Docker hosts to
send information to Weave Scope.  To do this, type
these commands:

# sudo wget -O /usr/local/bin/scope https://git.io/scope-latest
# sudo chmod a+x /usr/local/bin/scope
# sudo scope launch --service-token={{.ProbeToken}}
