apiVersion: v1
kind: ReplicationController
metadata:
  name: monitoring
  labels:
    name: monitoring
spec:
  replicas: 1
  selector:
    name: monitoring
  template:
    metadata:
      labels:
        name: monitoring
    spec:
      containers:
      - name: monitoring
        image: quay.io/weaveworks/monitoring
        env:
        - name: GET_HOSTS_FROM
          value: dns
        ports:
        - containerPort: 3000
        - containerPort: 9090
        - containerPort: 9093
