apiVersion: v1
kind: ReplicationController
metadata:
  name: pipe-master-4fa40e2
spec:
  replicas: 3
  selector:
    name: pipe
    version: master-4fa40e2
  template:
    metadata:
      labels:
        name: pipe
        version: master-4fa40e2
    spec:
      containers:
      - name: pipe
        image: weaveworks/scope:master-4fa40e2
        args:
        - --no-probe
        - --weave=false
        - --app.log.http=true
        - --app.http.address=:80
        - --app.userid.header=X-Scope-OrgID
        - --app.pipe.router=consul://consul.default.svc.cluster.local:8500/pipes/
        - --app.consul.inf=eth0
        env:
        - name: CHECKPOINT_DISABLE
          value: "1"
        - name: SCOPE_HOSTNAME
          value: "service"
        ports:
        - containerPort: 80
