apiVersion: v1
kind: ReplicationController
metadata:
  name: frontend-mt-a1c6606
spec:
  replicas: 3
  selector:
    name: frontend-mt
    version: a1c6606
  template:
    metadata:
      labels:
        name: frontend-mt
        version: a1c6606
    spec:
      imagePullSecrets:
      - name: quay-secret
      containers:
      - name: frontend
        image: quay.io/weaveworks/frontend-mt:a1c6606
        ports:
        - containerPort: 80
        - containerPort: 443
        volumeMounts:
        - name: tls-secret
          mountPath: /tls/secret/
          readOnly: true
      volumes:
      - name: tls-secret
        secret:
          secretName: tls-secret
