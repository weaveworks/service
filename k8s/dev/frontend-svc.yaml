apiVersion: v1
kind: Service
metadata:
  name: frontend
  labels:
    name: frontend
spec:
  ports:
    - port: 80
      name: http
    - port: 443
      name: https
  selector:
    name: frontend
  type: LoadBalancer
