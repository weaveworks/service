general:
  branches:
    ignore:
      - gh-pages

machine:
  services:
    - docker
  environment:
    GOPATH: /home/ubuntu
    TOOLS: /home/ubuntu/src/github.com/weaveworks/tools
    SRCDIR: /home/ubuntu/src/github.com/weaveworks/service
    PATH: $PATH:$HOME/bin

dependencies:
  post:
    - mkdir -p $TOOLS
    - git clone https://github.com/weaveworks/tools.git $TOOLS
    - go clean -i net
    - go install -tags netgo std
    - mkdir -p $(dirname $SRCDIR)
    - cp -r $(pwd)/ $SRCDIR
    - curl -L https://github.com/docker/compose/releases/download/1.3.3/docker-compose-`uname -s`-`uname -m` > $HOME/bin/docker-compose
    - chmod +x $HOME/bin/docker-compose

test:
  override:
    - cd $SRCDIR; $TOOLS/lint .
    - cd $SRCDIR; $TOOLS/test
    - cd $SRCDIR/users; make image.tar
    - cd $SRCDIR/app-mapper; make image.tar
    - cd $SRCDIR/client; $TOOLS/rebuild-image service_client_build . Dockerfile package.json karma.config.js karma.cross-browser.config.js webpack.local.config.js webpack.production.config.js
    - cd $SRCDIR/client; make tests
    - cd $SRCDIR/client; make image.tar

teardown:
  pre:
    - cd $SRCDIR; cp coverage.* $CIRCLE_ARTIFACTS
