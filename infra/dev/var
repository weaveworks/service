#
# AWS
#

# The credentials for the AWS account you want to use. For personal clusters,
# this can be your personal account. Most likely, it should be one of the
# shared Weaveworks accounts. This must be the same as `aws configure`!
#
AWS_ACCESS_KEY="AKIAIKG6BZNPFGV3JOLQ"
AWS_SECRET_KEY="h5+QGnarlAckNohMa2fDWY6WJCC96R1YXd9kBFAz"

# The region for your cluster. This must be the same as `aws configure`!
#
AWS_REGION="us-east-1"

#
# Kubernetes
#

# The default availability zone in your region.
# The kube-up script will stage some S3 assets here.
#
K8S_AZ="us-east-1a"

# The AMI that Kubernetes will use. Should be Ubuntu vivid hvm:ebs. Use the
# Ubuntu locator at http://cloud-images.ubuntu.com/locator/ec2/ to find the
# right image for your region.
#
K8S_AMI="ami-d92e6bb3"

# EC2 instance size for the Kubernetes master node.
# Should not be smaller than m3.medium.
#
K8S_MASTER_SIZE="m3.medium"

# EC2 instance size for the Kubernetes minion nodes. t2.micro is enough to
# boot Kubernetes for a test cluster, but not enough to run Scope-as-a-Service
# components. For that you need at least t2.medium.
#
K8S_MINION_SIZE="t2.medium"

# How many minion nodes to spawn.
#
K8S_NUM_MINIONS="3"

# A prefix for the EC2 instance names. Should probably include "kubernetes"
# and the name of the cluster, to help keep things understandable.
#
K8S_INSTANCE_PREFIX="kubernetes_dev_b"

#
# RDS
#

# We create multi-AZ RDS instances, which need to be members of at least 2
# subnets in different availability zones. Specify at least 2 distinct
# availability zones for your region, here.
#
RDS_SUBNET_A_AZ="us-east-1a"
RDS_SUBNET_B_AZ="us-east-1b"

# Passwords for the RDS databases. If you need help, try:
#
#  cat /dev/urandom | xxd -ps | head -c16
#
RDS_USERS_DB_PASSWORD="us45dcd40163798d"
RDS_APP_MAPPER_DB_PASSWORD="am1e28d6a4b3302b"

#
# Route53
#

# The zone name for this cluster, e.g. "cloud.weave.works" or
# "dev.weave.works".
#
R53_ZONE_NAME="dev.weave.works"

# The FQDN resource within the zone, e.g. "frontend.cloud.weave.works" or
# "frontend.dev.weave.works".
#
R53_RESOURCE_NAME="frontend.dev.weave.works"

# The endpoint for the ELB that sits in front of the Kubernetes frontend
# service. You won't have that information in the beginning, so leave it blank
# to start. Once you launch the frontend service in Kubernetes, get the
# endpoint via
#
#  kubectl describe svc frontend
#
R53_FRONTEND_ELB_ENDPOINT="a8a32499ed73d11e59bbe12dc9dc996a-1542465414.us-east-1.elb.amazonaws.com"

# The canonical zone ID for the ELB, above. Again, you won't have that
# information to start, so leave it blank. Once you launch the frontend
# service in Kubernetes, get the zone ID via
#
#  aws elb describe-load-balancers
#
R53_FRONTEND_ELB_ZONE_ID="Z3DZXE0Q79N41H"

